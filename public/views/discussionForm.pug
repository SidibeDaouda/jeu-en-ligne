include includes/head.pug
body
  include includes/header.pug
  section.hero
      .hero-body
        nav.tabs.is-boxed
          .container
            ul
              li.tab.is-active(onclick="openTab(event,'discussion')")
                a Discussion
              li.tab.tabMobile(onclick="openTab(event,'enLigneMobile')")
                a En ligne
              li.tab 
                a(href='/salon') 🚪
        .box.container(style='background-color: #fff; margin-top:-25px;')
          #discussion.content-tab
            .columns.is-gapless
              .column.is-2
                .title.is-5 En Ligne
                #listeUtilisateurs.nomCapital
              .column.customResponsive
                .chatBox
                form.form(id="envoyerMessage")
                  .field.has-addons
                    .control.is-expanded
                      input.input.has-fixed-size(placeholder='Écrivez quelque chose' required)
                    .control
                      button.button.is-primary
                        i.fa.fa-paper-plane(aria-hidden='true')
          #enLigneMobile.content-tab(style='display:none')
            #listeUsersMobile.nomCapital
  script(src='https://cdnjs.cloudflare.com/ajax/libs/moment.js/2.22.2/moment.min.js')
  script(src='https://cdnjs.cloudflare.com/ajax/libs/qs/6.6.0/qs.min.js')
  script(type='text/javascript' src='/socket.io/socket.io.js')
  script(src='/js/discussionClient.js')
  include includes/footer.pug